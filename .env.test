# Fixture-driven test environment. Loaded explicitly via `node --env-file=.env.test`
# or via the e2e vitest config — NOT auto-loaded by anything else.

GMAIL_FIXTURE_MODE=1
GMAIL_FIXTURE_DIR=./fixtures/gmail
GMAIL_CONFIG_DIR=./.test-config
GMAIL_ACCOUNT=work
